# Optional: your own HTTPS endpoint that accepts POST JSON and returns HTTP 2xx.
# Leave empty to keep enrollment and contact forms in honest local preview mode.
# Never put API secrets into VITE_ variables; these are exposed to browsers.
VITE_FORM_ENDPOINT=
